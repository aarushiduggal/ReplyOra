import { SectionScaffold } from "@/components/social/section-scaffold";

export default function ClientOverviewPage() {
  return (
    <SectionScaffold
      num="01"
      label="Overview"
      headline="The account cockpit — deliverables, status, at a glance."
      blurb="Setup checklist, package deliverables, platforms, pillars, feed preview and approval counts."
    />
  );
}
