import { SectionScaffold } from "@/components/social/section-scaffold";

export default function ClientReportsPage() {
  return (
    <SectionScaffold
      num="08"
      label="Reports"
      headline="The client-call dashboard, ready to present."
      blurb="Period comparison, top posts, pillar breakdown and an editable exec summary — PDF export."
    />
  );
}
